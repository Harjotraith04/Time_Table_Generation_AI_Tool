# =============================================================================
# MAILING SYSTEM CONFIGURATION
# =============================================================================
# Complete this file and rename it to .env to enable the mailing system

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
PORT=8000
HOST=localhost
NODE_ENV=development

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
MONGODB_URI=mongodb://localhost:27017/timetable_generator

# =============================================================================
# JWT AUTHENTICATION CONFIGURATION
# =============================================================================
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h

# =============================================================================
# CLIENT CONFIGURATION
# =============================================================================
CLIENT_URL=http://localhost:5173
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# =============================================================================
# EMAIL CONFIGURATION - REQUIRED FOR MAILING SYSTEM
# =============================================================================
# Enable email functionality (set to true to enable mailing)
EMAIL_ENABLED=true

# SMTP server configuration
# For Gmail, use these settings and enable 2FA + App Password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# Your email credentials
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# From email address for system emails
FROM_EMAIL=noreply@timetablegenerator.com

# =============================================================================
# GMAIL SETUP INSTRUCTIONS
# =============================================================================
# 1. Go to your Google Account settings
# 2. Enable 2-Factor Authentication
# 3. Generate an App Password:
#    - Go to Security > 2-Step Verification > App passwords
#    - Select "Mail" and your device
#    - Copy the generated 16-character password
#    - Use this password in SMTP_PASS (without spaces)
# 4. Set your Gmail address in SMTP_USER
# 5. Set EMAIL_ENABLED=true

# =============================================================================
# OTHER EMAIL PROVIDERS
# =============================================================================
# For Outlook/Hotmail:
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_SECURE=false

# For Yahoo:
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=587
# SMTP_SECURE=false

# For custom SMTP servers, adjust the settings accordingly
